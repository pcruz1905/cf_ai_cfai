{
  "name": "cfai",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter cfai-agent dev",
    "deploy": "pnpm --filter cfai-agent deploy",
    "test": "pnpm --filter cfai-agent test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "catalog:",
    "typescript": "^5.9.3",
    "vitest": "catalog:"
  }
}
